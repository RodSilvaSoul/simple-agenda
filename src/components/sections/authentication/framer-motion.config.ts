import { Variants } from "framer-motion";

export const formVariants: Variants = {
  enter: {
    opacity: 0,
    scale: 0,
  },
  center: {
    opacity: 1,
    scale: 1,
  },
  exit: {
    opacity: 0,
    scale: 0,
  },
};  